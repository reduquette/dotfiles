---
name: User environment
description: Claude Code runs on a remote Linux workspace machine, not the user's local machine
type: user
---

Claude Code runs on a remote Linux workspace machine (not the user's local laptop). This means `bypassPermissions` mode is safe to use and is appropriate for this environment.
